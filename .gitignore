.venv/
__pycache__/
*.pyc
.cache/
profile/
# Gecko profiler captures are huge (100s of MB each); the JSON results and
# report.html in results/ are checked in, the .profile.json captures are not.
results/*.profile.json
